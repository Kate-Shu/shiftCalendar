import { styled, Box } from '@mui/material'

export const StyledContainer = styled(Box)(() => ({
 marginTop: "2rem",
 backgroundColor: '#f4f1f0',
 zIndex: 2,
 position: "relative",
 paddingTop: '15px'
}))